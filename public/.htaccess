RewriteEngine On

# 🔁 Rewrite /storage/* to /build/storage/*
RewriteRule ^storage/(.*)$ build/storage/$1 [L]

# ⚙️ Laravel-style fallback to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php/$1 [L,QSA]
